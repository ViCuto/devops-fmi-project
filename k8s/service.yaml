apiVersion: v1
kind: Service
metadata:
  name: greeting-service
  namespace: greeting-app
spec:
  selector:
    app: greeting-app
  ports:
    - protocol: TCP
      port: 80         
      targetPort: 5000 
  type: LoadBalancer